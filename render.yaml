services:
  - type: web
    name: health-risk-predictor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: flet run app/main.py --web --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0